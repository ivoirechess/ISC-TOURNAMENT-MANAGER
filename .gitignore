# Local environment. env.js is committed: it only carries the Supabase URL
# and the anon (publishable) key, public by design. Any file that could
# hold real secrets stays out of the repository.
.env
.env.*
!.env.example

node_modules/

# Playwright artefacts (captures, traces) : jamais versionnes.
test-results/
playwright-report/
